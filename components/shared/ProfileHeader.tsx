const ProfileHeader = ({ 
    accountId,
    authUserId,
    name,
    username,
    imgUrl,
    bio,
 }) => {
    return ( 
        <div>
            <h1>Profile Header</h1>
        </div>
    );
}

export default ProfileHeader;